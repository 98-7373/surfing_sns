import 'package:flutter/material.dart';

class SurfSearchPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("surf")),
    );
  }
}
